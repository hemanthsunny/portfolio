import { SidebarStickyDirective } from './sidebar-sticky.directive';

describe('SidebarStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarStickyDirective();
    expect(directive).toBeTruthy();
  });
});
